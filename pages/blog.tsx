import type { NextPage } from "next";
import { Layout } from "@/components/layout";

const Blog: NextPage = () => {
  return (
    <Layout title="Blog">
      <p>Blog</p>
    </Layout>
  );
};

export default Blog;
